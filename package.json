{
  "name": "task-board",
  "version": "1.0.0",
  "scripts": {
    "dev": "run-p watch serve",
    "watch": "tsc -w",
    "serve": "live-server --open=src/index.html",
    "build": "tsc"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^5.0.0",
    "live-server": "^1.2.1"
  }
}
